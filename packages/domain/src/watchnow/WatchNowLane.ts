// "지금 볼 것" 화면의 칸 (docs/19-watch-now.md §화면: 역할이 고정된 칸 3개).
//
// **칸을 경쟁시키지 않으려고 만든 enum 이다.** 하나의 리스트에 모든 후보를 넣고 순위를
// 매기면 "P16 언더컷과 선두 교체 중 무엇이 위인가" 를 답해야 하는데 그 답은 데이터에
// 없다. 칸에 역할을 고정하면 그 비교 자체가 사라진다.
export enum WatchNowLane {
  // 1 선두권 — P1~P3. 포디움은 F1 규정이 정한 경계다.
  Leader = "leader",
  // 2 필드 — P4 이하 전체. 방송이 안 보여주는 나머지이며 이 앱의 존재 이유다.
  // "중위권" 으로 좁히지 않는다. P15~P20 이 다시 사라지면 근거가 무너진다.
  Field = "field",
  // 3 내 드라이버 — 즐겨찾기. 즐겨찾기가 비면 칸을 접는다.
  Favorite = "favorite",
}
