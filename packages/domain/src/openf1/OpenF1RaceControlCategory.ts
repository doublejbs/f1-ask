// OpenF1 race_control 응답의 category 필드 값.
// 실측(session_key=11334, 151행) 분포: Flag 108 / Other 35 / SafetyCar 6 / SessionStatus 2.
export enum OpenF1RaceControlCategory {
  Flag = "Flag",
  Other = "Other",
  SafetyCar = "SafetyCar",
  SessionStatus = "SessionStatus",
}
