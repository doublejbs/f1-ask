{
  "name": "f1-second-screen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.22.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @f1/web dev",
    "build": "pnpm --filter @f1/web build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "firebase:emulators": "firebase emulators:start"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "firebase-admin": "^14.2.0",
    "firebase-tools": "^13.35.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
